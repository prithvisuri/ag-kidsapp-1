version: '3.8'

services:
  web:
    build: ./frontend
    ports:
      - "8080:80"
    restart: always

  # backend:
  #   build: ./backend
  #   ports:
  #     - "3000:3000"
  #   restart: always
